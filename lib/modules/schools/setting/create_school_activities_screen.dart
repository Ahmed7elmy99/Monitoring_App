import 'package:flutter/material.dart';

class CreateSchoolActivitiesScreen extends StatelessWidget {
  const CreateSchoolActivitiesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create School Activities'),
      ),
      body: Center(
        child: Text('Create School Activities'),
      ),
    );
  }
}
